replicaCount: 1

image:
  repository: omarghonim/devops-vote
  tag: "latest"

containerPort: 8080

service:
  port: 80

resources:
  limits:
    cpu: 300m
    memory: 256Mi
  requests:
    cpu: 150m
    memory: 128Mi

redis:
  host: "redis"

postgres:
  host: "redis-master.prod.svc.cluster.local"
  password: "devpass"

ingress:
  enabled: true
  host: "vote.dev.local"
