apiVersion: v1
kind: Service
metadata:
  name: {{ include "vote-app-chart.fullname" . }}
  labels:
    {{- include "vote-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vote-app-chart.selectorLabels" . | nindent 4 }}
